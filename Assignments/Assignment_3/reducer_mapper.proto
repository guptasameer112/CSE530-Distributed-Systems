syntax = "proto3";

package reducer_mapper;

service ReducerMapper {
  rpc ReceivePartition(PartitionRequest) returns (EmptyResponse);
}

message PartitionRequest {
  repeated Partition partitions = 1;
}

message Partition {
  int32 centroid_id = 1;
  repeated float values = 2;
}

message EmptyResponse {}
